# -----------------------------------------------------------------------------
# Thesis_V1 — Python dependencies
# -----------------------------------------------------------------------------
# Tested with Python 3.12.3 on Ubuntu 22.04 (July 2026).
# Pinned to a date window to keep experiments reproducible.
# Regenerate by upgrading with: pip install -U <package>
# -----------------------------------------------------------------------------

# --- Core data science ---
numpy>=2.0,<3.0
pandas>=2.2,<3.0
matplotlib>=3.9,<4.0
seaborn>=0.13,<1.0

# --- Machine learning (ensemble models for the comparison study) ---
scikit-learn>=1.5,<2.0
xgboost>=2.1,<3.0
lightgbm>=4.5,<5.0

# --- Explainability (the heart of this thesis) ---
shap>=0.46,<1.0
lime>=0.2.0.1,<1.0

# --- Hyper-parameter optimisation ---
optuna>=4.0,<5.0

# --- Utilities ---
joblib>=1.4,<2.0
tqdm>=4.66,<5.0
scipy>=1.13,<2.0
statsmodels>=0.14,<1.0
imbalanced-learn>=0.12,<1.0        # SMOTE / class-imbalance tools

# --- Notebook / IDE integration ---
jupyter>=1.1,<2.0
ipykernel>=6.29,<7.0
notebook>=7.1,<8.0

# --- Config / secrets ---
python-dotenv>=1.0,<2.0
